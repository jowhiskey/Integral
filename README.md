# Integral

A standalone, isomorphic WYSIWYG math editor package: direct AST manipulation, MathML Core rendering, math.js conversion, engineering features (units, constants, host-provided variable picker).

**Status:** private, pre-release, under active initial development (AST core).

See `AGENTS.md` for contribution/agent rules and `docs/context/editor-spec.md` for the specification.
